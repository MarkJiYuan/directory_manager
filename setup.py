from setuptools import setup, version

setup(
    name='dirmanager',
    version='0.0.1',
    description='A small tool to help create folders & files',
    py_modules=["main"],
    package_dir={'': 'src'}
)